# ==========================================
# M4S Merger Tools - Dependency List
# M4S 文件处理工具 - 依赖列表
# ==========================================

# [GUI Framework / GUI 框架]
# Required for the graphical interface (gui.py)
# 必须：用于图形界面
customtkinter>=5.2.0

# [Image Processing / 图像处理]
# Required by customtkinter for rendering icons and images
# 推荐：CustomTkinter 内部依赖 Pillow 处理图像渲染
Pillow>=10.0.0

# [System / 系统工具]
# Usually installed automatically, but good to be explicit for CTK
# 推荐：用于版本解析，防止某些环境报错
packaging

# [Build Tool / 打包工具]
# Optional: Only needed if you want to build .exe files
# 可选：仅当需要生成 EXE 可执行文件时安装
pyinstaller>=6.0.0